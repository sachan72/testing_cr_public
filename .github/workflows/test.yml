name: testing demo

on: 
  push:
    branches: 
    - push-test
  pull_request: 
    branches: 
    - pull-req-test

jobs:
  display:
    runs-on: ubuntu-latest

    steps:
      - name: checkingout repos
        uses: actions/checkout@v4

      - name: display message 
        run: echo "testing done".
    
     

    

    
  
  

 
